---
name: Color
route: /SetColor
menu: Style
---

import { Playground, Props } from 'docz'
import { Label } from '../../src/core/label'
import { 
  ExampleColors, NatureColors,
  GrayscaleColors, StatusColors,
  ForEachColor
} from '../../src/core/utils'

# Uke Color

## Main Color

<Playground>
  {
    () => {
      return (
        <div className="layout">
          {
            ForEachColor(ExampleColors)((color, idx) => {
              return (
                <div key={color} className="flex text-center">
                  <div className={`flex bg_${color}`}
                    style={{
                      height: 150
                    }}>
                  </div>
                  {color}
                </div>
              )
            })
          }
        </div>
      )
    }
  }
</Playground>
