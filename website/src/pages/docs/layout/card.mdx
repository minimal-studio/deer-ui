---
name: Card
title: Card
menu: Layout
---

# Card

常用的布局容器

## Basic Usage

```jsx live noInline
import { Card, Grid } from '@deer-ui/core';

function Example() {
  return (
    <Card container className="bg_default">
      <Grid container space={5}>
        <Grid item xs={12} sm={6} lg={6}>
          <Card p={20}>
            deer-ui 是基于 React 的 UI 库，提供简约和功能齐全的组件，可高度定制的组件接口，灵活的配置，提供给开发者另一种开发思路，也致力于尝试不同的组件使用和开发方向。
          </Card>
        </Grid>
        <Grid item xs={12} sm={6} lg={6}>
          <Card p={20}>
            deer-ui 是基于 React 的 UI 库，提供简约和功能齐全的组件，可高度定制的组件接口，灵活的配置，提供给开发者另一种开发思路，也致力于尝试不同的组件使用和开发方向。
          </Card>
        </Grid>
        <Grid item xs={12} sm={4} lg={4}>
          <Card p={20}>
            deer-ui 是基于 React 的 UI 库，提供简约和功能齐全的组件，可高度定制的组件接口，灵活的配置，提供给开发者另一种开发思路，也致力于尝试不同的组件使用和开发方向。
          </Card>
        </Grid>
        <Grid item xs={12} sm={8} lg={8}>
          <Card p={20}>
            deer-ui 是基于 React 的 UI 库，提供简约和功能齐全的组件，可高度定制的组件接口，灵活的配置，提供给开发者另一种开发思路，也致力于尝试不同的组件使用和开发方向。
          </Card>
        </Grid>
        <Grid item xs={12} sm={8} lg={8}>
          <Card p={20}>
            deer-ui 是基于 React 的 UI 库，提供简约和功能齐全的组件，可高度定制的组件接口，灵活的配置，提供给开发者另一种开发思路，也致力于尝试不同的组件使用和开发方向。
          </Card>
        </Grid>
        <Grid item xs={12} sm={4} lg={4}>
          <Card p={20}>
            deer-ui 是基于 React 的 UI 库，提供简约和功能齐全的组件，可高度定制的组件接口，灵活的配置，提供给开发者另一种开发思路，也致力于尝试不同的组件使用和开发方向。
          </Card>
        </Grid>
      </Grid>
    </Card>
  )
}
render(<Example />);
```

## 模拟产品信息

```jsx live noInline
import { Card, Grid } from '@deer-ui/core';

function Example() {
  return (
    <Card
      container
      className="bg_default"
    >
      <Card
        style={{
          width: 300,
          margin: `10px auto`
        }}
      >
        <div
          style={{
            height: 100,
            lineHeight: `100px`,
            textAlign: 'center',
            backgroundColor: `#376BFB`,
            color: '#FFF'
          }}
        >
          产品图片
        </div>
        <Grid
          container
          space={10}
        >
          <Grid lg={12}>
            产品信息
          </Grid>
          <Grid lg={12}>
            价格 ¥ 10.00
          </Grid>
        </Grid>
      </Card>
    </Card>
  )
}
render(<Example />);
```

## 模拟键盘

```jsx live noInline
import React, { useState } from "react";
import { Card, Grid } from '@deer-ui/core';
import { GenerateNumberRange } from "@mini-code/base-func";

const keyboardNumbs = GenerateNumberRange([1, 9]);

const SimulationKeyboard = () => {
  const [inputVal, setinputVal] = useState(0);
  return (
    <Card container className="bg_default"
      style={{maxWidth: 400}}
    >
      <div
        className="bg_white p10"
      >
        <strong className="highlight">{inputVal}</strong>
      </div>
      <Grid
        container
        className="keyboard"
      >
        <Grid
          sm={9}
          xs={9}
        >
          <Grid
            container
            space={15}
          >
            {
              keyboardNumbs.map((num, idx) => {
                return (
                  <Grid
                    key={num}
                    xs={4}
                    sm={4}
                    onClick={(e) => {
                      setinputVal(+(+inputVal + num));
                      // changeMultiple(num);
                      // closeModal();
                    }}
                  >
                    <Card p={20} className="text-center">
                      {num}
                    </Card>
                  </Grid>
                );
              })
            }
          </Grid>
        </Grid>
        <Grid
          sm={3}
          xs={3}
        >
          <Grid
            container
            space={15}
          >
            <Grid
              sm={12}
              xs={12}
              onClick={(e) => {
                let nextNum = String(inputVal).substring(0, String(inputVal).length - 1);
                if(!nextNum) nextNum = 0;
                setinputVal(nextNum);
              }}
            >
              <Card p={20} className="text-center">
                X
              </Card>
            </Grid>
            <Grid
              sm={12}
              xs={12}
              onClick={(e) => {
                alert(inputVal);
              }}
            >
              <Card p={20} className="text-center">
                确定
              </Card>
            </Grid>
          </Grid>
        </Grid>
      </Grid>
    </Card>
  );
};

render(<SimulationKeyboard />)
```
