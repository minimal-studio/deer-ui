---
name: Style
route: /SetStyle
menu: Style
---

# Style

## Import Style

在项目入口 `.scss` 文件 `import` `deer-ui` 的内置样式

```scss
// 默认的颜色变量
@import '@deer-ui/all/style/color/default.scss';

// 依赖颜色变量的主样式入口
@import '@deer-ui/all/style/var.scss';
@import '@deer-ui/all/style/index.scss';
```

## 自定义样式变量

创建 `./var.scss` 变量 scss 文件，具体内容参考 https://github.com/minimal-studio/deer-ui/blob/master/style/color/default.scss

```scss
@import '@deer-ui/all/style/color/default.scss';

// 在 deer-ui/style/index.scss 之前覆盖 color 变量
@import './var.scss';

// 依赖颜色变量的主样式入口
@import '@deer-ui/all/style/index.scss';
```

### 3. 使用 color class

规则

1. bg_ 前缀设置背景颜色，bg_red
1. t_ 前缀设置字体颜色，t_green

```jsx
<div className="bg_red"></div>
```
