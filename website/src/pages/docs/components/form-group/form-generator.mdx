---
name: FormGenerator
route: /FormGenerator
menu: FormGenerator
---

import { Playground, Props } from 'docz'
import { FormGenerator } from '@dear-ui/enhance-ui/form-generator';
import { Captcha } from '@dear-ui/enhance-ui/captcha';
import formOptions from '@dear-ui/enhance-ui/form-layout/demo/form-options-demo';

# FormGenerator

## Usage

<Playground>
  {
    () => {
      Captcha.setAPI(
        (callback) => {
          setTimeout(() => {
            callback({
              hasErr: false,
              captchaImage: 'https://cms.marketplace.org/sites/default/files/styles/primary-image-400x222/website/static/captcha.png?itok=JsLPSa66',
              captchaKey: '12412412512513'
            })
          }, 1000);
        }
      )
      let formRef;
      return (
        <div>
          <div>
            <FormGenerator
              ref={e => formRef = e}
              onChange={(values, ref, val) => {
                console.log(values, ref, val)
              }}
              formOptions={formOptions}/>
          </div>
          <span className="btn theme flat mr10" onClick={e => {
              console.log(formRef, formRef.value);
          }}>提交</span>
          <span className="btn red flat" onClick={e => {
              formRef.clearValue();
          }}>清空表单</span>
        </div>
      )
    }
  }
</Playground>

## Props

<Props of={FormGenerator} />
