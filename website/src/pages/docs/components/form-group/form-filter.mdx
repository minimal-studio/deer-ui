---
name: FormFilter
route: /FormFilter
menu: Form Generator
---

import { Playground, Props } from 'docz'
import { FormFilter } from '@deer-ui/core/form-generator';

# FormFilter

> 表单生成器基础类，提供表单的基础验证方法

<Props of={FormFilter} />

```jsx static
import { FormFilter } from '@deer-ui/all';

class Form extends FormFilter {
  render() {
    return (
      ...do something
    )
  }
}
```
