---
name: InputSelector
title: InputSelector
menu: Form
---

# InputSelector

> 选择器 & 输入控件

## Basic Usage

```jsx live noInline
import { InputSelector } from '@deer-ui/core';

function Example() {
  return (
    <InputSelector
      values={{
        value1: 'value1',
        value2: 'value2',
        value3: 'value3',
      }}
      onChange={(inputVal, selectorVal) => {
        console.log(inputVal, 'inputVal')
        console.log(selectorVal, 'selectorVal')
      }} />
  )
}
render(<Example />);
```

## 选择器弹出方向控制

> 选择器为 Dropdown

```jsx live noInline
import { InputSelector } from '@deer-ui/core';

function Example() {
  return (
    <>
      <h3>top left</h3>
      <InputSelector
        position="top"
        values={{
          value1: 'value1',
          value2: 'value2',
          value3: 'value3',
        }}
        onChange={(inputVal, selectorVal) => {
          console.log(inputVal, 'inputVal')
          console.log(selectorVal, 'selectorVal')
        }} />
      <hr />  
      <h3>top right</h3>
      <InputSelector
        position="top,right"
        values={{
          value1: 'value1',
          value2: 'value2',
          value3: 'value3',
        }}
        onChange={(inputVal, selectorVal) => {
          console.log(inputVal, 'inputVal')
          console.log(selectorVal, 'selectorVal')
        }} />
    </>
  )
}
render(<Example />);
```
