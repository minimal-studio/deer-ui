---
name: Countdown
route: /Countdown
menu: Other / 其他组件
---

import { Playground, PropsTable } from 'docz'
import { CountdownFlip } from './';

import '../../../style/other/default.scss'

# CountdownFlip

<PropsTable of={CountdownFlip} />

## Usage

### Import Component

```js
import Countdown from 'ukelli-ui/other/countdown/countdown';
```

### Import Style

```scss
@import 'ukelli-ui/style/other/countdown.scss'
```

<Playground className="bg_default">
  {
    () => {
      return (
        <div>
          <CountdownFlip
            start={66}
            height={40}
            width={60}
            onTimeout={e => {
              console.log(e)
            }} />
          <hr />
          <CountdownFlip
            start={66}
            needProgress
            onTimeout={e => {
              console.log(e)
            }} />
        </div>
      )
    }
  }
</Playground>
