import React from 'react';
import Captcha from './captcha';

const CAPTCHA = (props) => {
  console.wran(window.$UKE.$T_UKE('CAPTCHA 名字将要废弃，请使用 Captcha'));
  return (
    <Captcha {...props}/>
  );
};

export {
  Captcha, CAPTCHA
};
