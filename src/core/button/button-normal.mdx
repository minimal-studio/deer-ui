---
name: Button
route: /Button
menu: Components / 组件
---

import { Playground, Props } from 'docz'
import { State, Toggle } from 'react-powerplug'
import { ShowModal } from '../modal';
import { Button } from './';

# Button

## Usage

<Playground>
  {
    () => {
      return (
        <State initial={{
          loading: false,
        }}>
          {
            ({ state, setState }) => {
              return (
                <div>
                  <Button
                    className="mr10"
                    onClick={e => {
                      ShowModal({
                        title: '试试',
                        children: (
                          <div className="p20">你好</div>
                        )
                      })
                    }} icon="archway">
                    弹窗
                  </Button>
                  <hr />
                  <Button
                    onClick={e => {
                      setState({
                        loading: !state.loading
                      })
                      setTimeout(() => {
                        setState(({ loading }) => ({
                          loading: !loading
                        }))
                      }, 1500)
                    }}
                    loading={state.loading}
                    icon="bolt"
                    color="green"
                    text="试试切换 Loading" />
                  <hr />
                  <Button
                    onClick={e => {
                      alert('你好')
                    }}
                    icon="bell-slash"
                    color="red"
                    disabled
                    text="禁用按钮" />
                  <hr />
                  <Button
                    onClick={e => {
                      alert('你好')
                    }}
                    icon="bell-slash"
                    className="res"
                    disabled
                    text="响应式按钮" />
                </div>
              )
            }
          }
        </State>
      )
    }
  }
</Playground>

-----------

## Style

实心的按钮样式 btn

<Playground>
  <button className="btn theme m5">按钮</button>
  <button className="btn red m5">按钮</button>
  <button className="btn gold m5">按钮</button>
  <button className="btn black m5">按钮</button>
  <button className="btn default m5">按钮</button>
  <button className="btn yellow m5">按钮</button>
  <button className="btn light-p m5">按钮</button>
  <button className="btn important m5">按钮</button>
  <button className="btn warn m5">按钮</button>
  <button className="btn wine m5">按钮</button>
  <button className="btn green m5">按钮</button>
  <button className="btn grey m5">按钮</button>
</Playground>

-----------

中空的按钮 hola

<Playground>
  <button className="btn hola theme m5">按钮</button>
  <button className="btn hola red m5">按钮</button>
  <button className="btn hola gold m5">按钮</button>
  <button className="btn hola black m5">按钮</button>
  <button className="btn hola default m5">按钮</button>
  <button className="btn hola yellow m5">按钮</button>
  <button className="btn hola light-p m5">按钮</button>
  <button className="btn hola important m5">按钮</button>
  <button className="btn hola warn m5">按钮</button>
  <button className="btn hola wine m5">按钮</button>
  <button className="btn hola green m5">按钮</button>
  <button className="btn hola grey m5">按钮</button>
</Playground>

-----------

链接按钮 link

<Playground>
  <span className="link-btn m5">按钮</span>
  <span className="link m5">按钮</span>
</Playground>

-----------

响应式按钮

<Playground>
  <button className="btn theme res">按钮</button>
</Playground>

## Props

<Props of={Button} />
