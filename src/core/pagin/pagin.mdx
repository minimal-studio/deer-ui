---
name: Pagination
route: /Pagination
menu: Form / 表单
---

import { Playground, Props } from 'docz'
import { State, Toggle } from 'react-powerplug'
import { Pagination } from './';

# Pagination

<Props of={Pagination} />

<Playground>
  <State initial={{
    pagingInfo: {
      active: true,
      total: 1000,
      pIdx: 0,
      pSize: 10
    },
    paginTransfer: {
      active: 'active',
      total: 'total',
      pIdx: 'pIdx',
      pSize: 'pSize'
    }
  }}>
    {
      ({ state, setState }) => (
        <Pagination
          onPagin={nextPagin => {
            setState({
              pagingInfo: nextPagin
            })
          }}
          infoMapper={state.paginTransfer}
          pagingInfo={state.pagingInfo}/>
      )
    }
  </State>
</Playground>

```js
class ForPagin extends React.Component {
  constructor(props) {
    super(props);

    this.state = {
      pagin: {
        PageIndex: 0,
        PageSize: 10,
        AllCount: 101,
        UsePaging: true
      }
    }
  }
  render() {
    const { pagin } = this.state;
    return (
      <Pagination
        onPagin={nextPagin => {
          this.setState({
            pagin: nextPagin
          })
        }}
        pagingInfo={pagin}/>
    )
  }
}

<ForPagin />
```
