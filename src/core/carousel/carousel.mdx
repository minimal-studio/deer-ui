---
name: Carousel
route: /Carousel
menu: Components
---

import { Playground, Props } from 'docz'
import { Carousel } from './';
import { Button } from '../button';

# Carousel

> 可以参考默认的动画，设置自己的动画

## Basic Usage

<Playground>
  <Carousel
    styleConfig={{
      width: '100%',
      height: 500
    }}
    carouselItems={[
      {
        imgUrl: '/public/images/c1.jpg',
        action: () => alert('点击了 img')
      },
      {
        imgUrl: '/public/images/c1.jpg',
        action: () => alert('点击了 img')
      },
      {
        element: (
          <div className="p20 t_white">
            使用自定义的 React Components
            <Button text="任意的" onClick={e => alert('点击了按钮')} />
          </div>
        )
      }
    ]}/>
</Playground>

## Props

<Props of={Carousel} />
