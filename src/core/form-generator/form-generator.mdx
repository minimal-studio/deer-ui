---
name: FormGenerator
route: /FormGenerator
menu: FormGenerator / 表单生成
---

import { Playground, PropsTable } from 'docz'
import { FormGenerator } from './';

import formOptions from './form-options-demo';
import FormOptionsDesc from './form-options-demo.md';

# FormGenerator

<PropsTable of={FormGenerator} />

<Playground>
  {
    () => {
      /** formOptions 查看 ./form-options-demo.js */
      let formRef;
      return (
        <div>
          <FormGenerator
            ref={e => formRef = e}
            onChange={(values, ref, val) => {
              console.log(values, ref, val)
            }}
            formOptions={formOptions}/>
          <span className="btn theme flat" onClick={e => {
              formRef.clearValue();
          }}>清空表单</span>
        </div>
      )
    }
  }
</Playground>

<FormOptionsDesc />