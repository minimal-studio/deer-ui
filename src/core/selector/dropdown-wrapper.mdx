---
name: DropdownWrapper
route: /DropdownWrapper
menu: Utils
---

import { Playground, PropsTable } from 'docz'
import { State, Toggle } from 'react-powerplug'
import DropdownWrapper from './dropdown-wrapper';

# DropdownWrapper

> 通用的下拉组件，可以渲染任意子内容

<PropsTable of={DropdownWrapper} />

<Playground>
  {
    () => {
      return (
        <State initial={{}}>
        {
          ({ state, setState }) => {
            return (
              <DropdownWrapper menuTitle="标题">
                {
                  (options) => {
                    console.log(options)
                    return (
                      <div className="p10" style={{width: 400}}>
                        任意内容
                      </div>
                    )
                  }
                }
              </DropdownWrapper>
            )
          }
        }
        </State>
      )
    }
  }
</Playground>

## 自定义外层显示内容

<Playground>
  {
    () => {
      return (
        <State initial={{}}>
        {
          ({ state, setState }) => {
            return (
              <DropdownWrapper menuWrapper={() => {
                return (
                  <div>显示内容</div>
                )
              }}>
                {
                  (options) => {
                    console.log(options)
                    return (
                      <div className="p10" style={{width: 400}}>
                        任意内容
                      </div>
                    )
                  }
                }
              </DropdownWrapper>
            )
          }
        }
        </State>
      )
    }
  }
</Playground>
