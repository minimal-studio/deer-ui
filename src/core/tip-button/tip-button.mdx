---
name: TipButton
route: /TipButton
menu: Components / 组件
---

import { Playground, Props } from 'docz'
import { TipButton } from './';

# TipButton

<Props of={TipButton} />

<Playground>
  <TipButton
    text="弹出层按钮"
    popover={(
      <div className="p15">弹出层的内容</div>
    )}
    timer={10000}
    onClick={e => {
      console.log(e)
    }}
  />
</Playground>
