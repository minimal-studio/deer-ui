---
name: DateShortcut
---

import { Playground, PropsTable } from 'docz'
import { State, Toggle } from 'react-powerplug'
import { DateShortcut } from './';

# DateShortcut

<PropsTable of={DateShortcut} />

<Playground style={{height: 450}}>
  <h4>输出字符串结果, 自带时间</h4>
  <State initial={{ res: [] }}>
    {
      ({ state, setState }) => (
        <div>
          返回值: {state.res.join(' - ')}
          <DateShortcut style={{width: 300}}
            outputAsString={true}
            onClick={e => setState({
              res: e
            })}/>
        </div>
      )
    }
  </State>
  <hr/>
  <h4>输出字符串结果, 不需要时间</h4>
  <State initial={{ res: [] }}>
    {
      ({ state, setState }) => (
        <div>
          返回值: {state.res.join(' - ')}
          <DateShortcut style={{width: 300}}
            outputAsString={true}
            needTime={false}
            onClick={e => setState({
              res: e
            })}/>
        </div>
      )
    }
  </State>
  <hr/>
  <h4>输出原生 Date 对象</h4>
  <State initial={{ res: [] }}>
    {
      ({ state, setState }) => (
        <div>
          返回值: {state.res.join(' - ')}
          <DateShortcut style={{width: 300}}
            onClick={e => setState({
              res: e
            })}/>
        </div>
      )
    }
  </State>
</Playground>
