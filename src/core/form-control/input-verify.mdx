---
name: InputVerify
route: /InputVerify
---

import { Playground, PropsTable } from 'docz'
import { InputVerify } from './';

# InputVerify

> 聚合输入框，包含以下功能

- 数字验证
- 数字范围限制
- 输入长度限制
- 百分比转换
- 数字转中文等一些列功能

<PropsTable of={InputVerify} />

<Playground>
  <InputVerify
    onChange={e => {}}
    onFocus={e => {}}
    onBlur={e => {}}
    onClear={e => {}}
    className={''}
    defaultValue={''}
    needCN={false}
    precent={false}
    inputable={true}
    numRange={[1, 100]}
    unit={2}
    lenRange={[1, 20]}
    onChange={e => {
      console.log(e)
    }} />
</Playground>
